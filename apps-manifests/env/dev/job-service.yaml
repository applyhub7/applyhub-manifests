image:
  name: noseyug/applyhub-job-service
  tag: "6d0c9f6"
container:
  port: 4002
configmap:
  enabled: true
  data:
    NODE_ENV: "development"
    JOB_PORT: "4002"
    JOB_DB_HOST: "postgres-svc.databases.svc.cluster.local"
    JOB_DB_PORT: "5432"
    JOB_DB_NAME: "job_service"
externalsecret:
  enabled: true
  secretStoreRef:
    name: aws-sm-css
    kind: ClusterSecretStore
  target:
    creationPolicy: Owner
  refreshInterval: 1h
  remoteKey: dev/applyhub/job-service
  properties:
    - JOB_DB_USER
    - JOB_DB_PASSWORD
