apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: job-postgres-statefulset
spec:
  serviceName: "job-postgres-service"
  replicas: 1
  selector:
    matchLabels:
      app: job-postgres-statefulset
  template:
    metadata:
      labels:
        app: job-postgres-statefulset
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: job-configmap
                  key: JOB_DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: job-configmap
                  key: JOB_DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: job-configmap
                  key: JOB_DB_PASSWORD
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data

      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: job-postgres-pvc
