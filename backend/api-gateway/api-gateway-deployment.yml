apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway-deployment
  template:
    metadata:
      labels:
        app: api-gateway-deployment
    spec:
      containers:
        - name: backend
          image: noseyug/applyhub-api-gateway:dev-e11689e
          ports:
            - containerPort: 4000
          envFrom:
            - configMapRef:
                name: api-gateway-configmap
