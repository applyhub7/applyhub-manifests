apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: application-service-postgres-es
  namespace: databases
  labels:
    app: application-service-postgres
    tier: database
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-sm-css
    kind: ClusterSecretStore
  target:
    name: application-service-postgres-sec
    creationPolicy: Owner
  data:
  - secretKey: POSTGRES_USER
    remoteRef:
      key: dev/applyhub/application-service
      property: APPLICATION_DB_USER
  - secretKey: POSTGRES_PASSWORD
    remoteRef:
      key: dev/applyhub/application-service
      property: APPLICATION_DB_PASSWORD
