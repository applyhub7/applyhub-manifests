apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: job-service-postgres-es
  namespace: databases
  labels:
    app: job-service-postgres
    tier: database
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-sm-css
    kind: ClusterSecretStore
  target:
    name: job-service-postgres-sec
    creationPolicy: Owner
  data:
  - secretKey: POSTGRES_USER
    remoteRef:
      key: dev/applyhub/job-service
      property: JOB_DB_USER
  - secretKey: POSTGRES_PASSWORD
    remoteRef:
      key: dev/applyhub/job-service
      property: JOB_DB_PASSWORD
