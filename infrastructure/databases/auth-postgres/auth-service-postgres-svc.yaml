apiVersion: v1
kind: Service
metadata:
  name: auth-service-postgres-svc
  namespace: databases
  labels:
    app: auth-service-postgres
    tier: database
spec:
  clusterIP: None
  ports:
    - port: 5432
  selector:
    app: auth-service-postgres
