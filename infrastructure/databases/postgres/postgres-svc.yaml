apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: databases
  labels:
    app: postgres
    tier: database
spec:
  clusterIP: None
  ports:
    - port: 5432
  selector:
    app: postgres
